---
/**
 * Drop-in reveal-on-scroll wrapper. Adds [data-reveal] so the global
 * IntersectionObserver (in Base.astro) picks it up.
 */

interface Props {
  delay?: number;
}

const { delay = 0 } = Astro.props;
---

<div data-reveal style={`transition-delay: ${delay}ms`}>
  <slot />
</div>
